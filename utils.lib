#!/bin/bash

function init {
    repo=${HOME}/repo
    busybox=${repo}/busybox

    if [ -z $linux ]; then
	linux=${repo}/linux
    fi
    vmlinux=${linux}/vmlinux
}

function die {
    echo $1
    exit 1
}
